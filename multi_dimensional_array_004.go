package main

func main() {
	var x int = 21      // Data type is inferred as int
	name := "Anirudhra" // Data type is not inferred
	// var can be used outside the function
	// := cannot be used outside the function
}
